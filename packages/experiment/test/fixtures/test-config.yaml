# Test configuration

models:
  - name: "test-model"  # Required: unique name
    model: "test-model"
    provider: "test"
    capabilities: ["test"]
    enabled: true

drivers:
  test: {}

evaluation:
  enabled: false

modules:
  - name: test-module
    path: ./test-module.ts
    description: Test module

testCases:
  - name: Test Case 1
    description: Simple test case
    input:
      query: test input
      context: some context

evaluators: []
